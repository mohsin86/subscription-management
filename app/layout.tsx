import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mohammed Mohasin — Senior Full Stack Developer",
  description:
    "Portfolio and CV of Mohammed Mohasin, a senior full stack developer specializing in Next.js, Node.js, and PHP.",
};

/**
 * RootLayout — top-level HTML shell for the whole app.
 * Args: children (React.ReactNode). Returns: <html>/<body> JSX wrapping children with Providers.
 */
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
