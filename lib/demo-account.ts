/**
 * DEMO_EMAIL — the seeded demo account's email, shared wherever demo-account
 * restrictions (e.g. disabling delete) need to check the current user.
 */
export const DEMO_EMAIL = "demo@subscription-tracker.dev";
